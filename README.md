the main idea 


what problem does it solve ?
insted of coding in a editor make something like blocks and you write code in that block and join these blocks which you want these block represent functions and if you want you can bind many of these in a class not like a block in a block but a thread binding functions of a class together, and then obsidian like graph network ,representing the function which are connected,  the central node is the main and variable functions and these are directly connected to function which are there in the main file for eg in js the main file is server.js so add this as central node and all file listed in it ... ,the main purpose of this project was to make different people code at same place so let every user code in a block and then link to the the code base
