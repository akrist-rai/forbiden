{
  "name": "forbiden",
  "version": "1.0.0",
  "private": true,
  "description": "FORBINDEN — Graph-based collaborative code IDE",
  "scripts": {
    "dev":         "concurrently --names \"API,WEB\" --prefix-colors \"cyan,magenta\" \"bun --dns-result-order=ipv4first --watch apps/api/src/server.ts\" \"sh -c 'cd apps/web && bun x vite'\"",
    "build":       "bun --cwd apps/web run build",
    "install:all": "bun install && bun install --cwd apps/api && bun install --cwd apps/web",
    "db:push":     "bun --cwd apps/api run db:push",
    "db:studio":   "bun --cwd apps/api run db:studio"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
